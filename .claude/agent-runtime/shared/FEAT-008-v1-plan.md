# FEAT-008 v1 Plan

- Expand `backend/prisma/schema.prisma` with the full Phase 1.1 MVP schema, enums, relations, and indexes.
- Generate the initial Prisma migration in `backend/prisma/migrations/`.
- Validate the Prisma schema locally and apply the migration once a valid `DATABASE_URL` is available in the workspace.
