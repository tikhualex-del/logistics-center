import { CouriersList } from '@/features/couriers'

export default function CouriersPage(): React.ReactElement {
  return <CouriersList />
  /*
    <div className="flex flex-col h-full">
      <header className="flex items-center h-14 px-6 border-b border-border bg-card shrink-0">
        <h1 className="text-sm font-semibold">Couriers</h1>
      </header>
      <main className="flex-1 flex items-center justify-center text-muted-foreground text-sm">
        Couriers list — coming in Phase 8 (Task 8.1a)
      </main>
    </div>
  */
}
